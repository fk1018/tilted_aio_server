export const getBrowserProfiles = (): any => {
	// const bpPath = path.join(__dirname, 'data/browser_profiles');
	// const dirs = readdirSync(bpPath, { withFileTypes: true })
	// 	.filter((dirent) => dirent.isDirectory())
	// 	.map((dirent) => dirent.name);
	// return dirs;
	return [];
};
